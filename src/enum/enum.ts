export enum StatusUserEnum {
    user = 1,
    manager = 10,
    admin = 100,
}

export enum JwtExpire {
    meiaHora = "30m",
    umaHora = "1h",
    duasHoras = "2h",
}

export enum JwtValidEnum {
    VALID = 0,
    INVALID = 1,
}
